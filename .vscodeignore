node_modules/
.vscode-test/
src/
tsconfig.json
esbuild.js
.eslintrc.json
**/*.ts
**/*.map
!dist/**
# unity-bridge/ and dist/unity-mcp.js are intentionally INCLUDED in the VSIX
# unity-bridge/      → copied into Unity project by installBridge command
# dist/unity-mcp.js  → bundled MCP server (ws inlined) referenced at runtime
